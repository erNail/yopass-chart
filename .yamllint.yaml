---
extends: "default"

ignore: |
  chart/templates/
  sealed-secret*
  chart/dist/
  !chart/templates/*-test.yaml

rules:
  line-length:
    max: 120
    level: "error"
  document-start:
    level: "error"
  quoted-strings:
    quote-type: "double"
    level: "error"
